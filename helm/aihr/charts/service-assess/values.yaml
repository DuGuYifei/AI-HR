replicaCount: 1

image:
  # 默认拼接：ghcr.io/<user>/<repo>-service-assess:<tag>
  repository: ""
  tag: ""
  pullPolicy: IfNotPresent

service:
  port: 8083

# 环境变量（非敏感）
env:
  springProfile: docker
  corsAllowed: ""
  jwtPublicKey: ""
